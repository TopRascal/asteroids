image_speed = 0; //sprite locked at 0 frames per second; is a static image

image_index = irandom(2);

direction = irandom(360); //object can spawn at any angle within 360 degrees

speed = 7; //base speed is 0.5, max is 2